apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mstockpartnerwebsitenextjs.fullname" . }}-test-connection"
  labels:
    {{- include "mstockpartnerwebsitenextjs.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mstockpartnerwebsitenextjs.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
