apiVersion: v1
kind: Service
metadata:
  name: {{ include "mstockpartnerwebsitenextjs.fullname" . }}
  labels:
    {{- include "mstockpartnerwebsitenextjs.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mstockpartnerwebsitenextjs.selectorLabels" . | nindent 4 }}
